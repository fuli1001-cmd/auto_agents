# dd9bafb1：最终镜像触发的追踪测试不匹配

2026-09-16 09:11 的恢复确实进入集中验收，没有重新规划或实施；在
`tests/test_gate_execution.py::test_auto_result_cache_reuses_when_only_unobserved_source_changes`
失败，独立审查被取消，耗尽预算的事务随后停止。

该测试用 `strace` 是否存在决定是否直接返回。候选已将可复用输入追踪切换到真实
metadata/trace owner，但测试仍在无 owner 的普通进程中期待完整追踪。
上次最终镜像新增 strace 后激活了这个路径，而此前的全量检查使用旧镜像，后续
仅复测已知失败与改动模块，遗漏了这个被环境变化激活的测试。

本次移除静默返回，独立运行时通过生产监督边界运行原测试全部断言，已有 owner
时直接运行。保留缓存复用、读取内容改变后缓存失效和失败行为检查，不伪造追踪。
修正候选的该文件在最终镜像中 15 项通过。

控制器停止信息现在先给出当前失败的节点或验收单元，再说明预算已耗尽，避免
只显示笼统的预算错误。控制器及交付回归 46 项通过。原预算与正式验收要求保留。

完整验收须在同一最终源码和镜像上完成；增量结果不能代替这一步。

本次真实 Codex 独立审查又验证了一个引入的审计回归：旧审计把 `and` 条件拆成
无序多重集合，错误接受 `ready() and prepare()` 改成 `prepare() and ready()`，
后者可通过副作用把原失败测试变成成功。现按原顺序保留每条断言的完整前缀，
仅允许追加条件；每个匹配消耗一次，防止一个断言代替多次原求值。真实执行反例、
前插/中插/重排、重复求值保留以及合法尾部增强测试通过，审计与控制器共 61 项通过。

第一次完整正式检查已完成 229 批、4544 个测试并获 Codex 批准，但验收复核发现
行为对照的旧实现把旧源码上的收集错误也视为成功反例，且 counterexamples 为空。
该验收已撤回，保留测试和审查证据。对照改为按模块独立运行，仅计入确实在测试体
执行阶段失败的节点；收集错误及 setup/teardown 错误不证明行为回归。新增驱动证据
区分 call_failed，相关对照、取消、存储与交付测试共 45 项通过。

后续独立审查验证了 strict XPASS 的特殊报告：测试体通过，但 pytest 将报告标为
failed 且不设置 wasxfail。可信驱动现通过 makereport 获取真实 call.excinfo，
只记录实际测试体异常。真实 pytest 子进程覆盖 strict XPASS、预期失败、普通断言、
unittest、setup 与 teardown，相关检查共 47 项通过。

独立审查进一步验证 unittest 将 setUp/tearDown 异常也归入 call 阶段。
驱动现要求真实异常回溯包含收集到的测试函数代码；未知自定义节点不提供反例。
真实子进程增加同步 setup/teardown/cleanup 和异步 setup/teardown 对照，保留同步、
异步测试体断言失败的正例；上述相关检查仍全部通过（47 项）。
