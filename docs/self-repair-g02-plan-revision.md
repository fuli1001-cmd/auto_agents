# 第 95 轮方案修订

[机器可读草案](self-repair-g02-plan-revision.json)针对独立审查 `f26bde7baec14642bc6596609e110742` 指出的注册链接缺口。
草案绑定候选 `607f2483c82b12938ff33c97ad9da2d1f164b853` 和原方案摘要，状态为未批准，后续仍须独立审查。

本次修改：

- 步骤 6 明确区分执行器登记的链接和不支持的项目链接：在实际遍历时检查准确路径、预期目标及只读依赖绑定，保留解析后的共享输入，登记目标变化须使缓存失效。不能用名称或前缀匹配直接放行，也不能排除整个依赖子树。
- 步骤 7 在原复用助手中加入真实安装的 `.conda` 链接与共享读取，覆盖 `observed_inputs`、`auto`、无关改动不重跑、真实输入变化后的执行失败，以及不完整运行后的健康恢复。原私有操作成功和共享修改拒绝必须同时保持。
- 步骤 5、8 将环境传递和 launcher 解析明确标为保留已有实现；步骤 6 同时保留已完成的 bookkeeping 排除修复。消除把旧缺陷仍写成待实现事项的表述。
- 三个相关场景补齐注册链接和恢复要求。全部 13 个场景、原验收与快速检查命令、`quick_required`、义务 ID 和原写入范围均保持。
- 当前诊断改为注册链接反例；旧 launcher 探针的原预期和 `already_fixed` 审查记录留在草案的 `basis` 中，没有改写历史。

恢复入口使用明确的 `self_repair_manual_plan` 来源记录。手工草案不能签发批准，旧批准也不能覆盖新草案。
原完成证据保存在历史中，当前修订仍需要新的独立审查和验收；尝试计数、既有批准和候选代码不因此重置或改写。

验证结果：

- 81 项针对性测试通过；随后全部 `tests/test_repair_*.py` 的 563 项测试在隔离验证机制下通过。
- 真实存档的私有副本装入修订后，直接进入 `self_repair_plan_review`，没有先生成另一份方案。
- 新负向诊断在当前候选中得到预期的行为失败，完全相同的诊断在父版本 `5dad474` 中通过。两个保留的正向探针合计 8 项通过。
- 全部原场景和命令保持。该回放没有调用模型，没有修改原候选或原实验状态。

安装草案只更新已停止引擎任务的规划记录，不启动 worker、调用模型、修改候选代码或恢复 sdgp。
`scripts/revise_repair_plan.py` 默认只验证，显式 `--apply` 才保存草案；它检查任务停止、进程退出、源版本、契约、父方案及验收保留，并保留修改前的实验备份。
修订已安装到停止的任务 `55b5e9bc4270493fb66d797a`，新请求为 `3c10aa8871154b6cb35e57c7d3e200b3`，
草案记录为 `9200431532204b489cfeb0c27b98022a`。任务仍为 `blocked`，候选数保持 94，候选源码、既有批准、范围结论和历史候选记录均未改变。
原命令下次恢复该修复任务时，将使用新草案进入独立审查。本次未启动修复或模型，也没有批准或实施这个候选。
实际安装、备份位置和验证结果见[验证记录](self-repair-g02-plan-revision-validation.json)。

诊断与测试材料：`/tmp/self-repair-plan-revision-92b1r791/` 下的 `rehearsal-current/report.json`、
`parent-control-result.json` 和 `repair-regressions/result.json`。
