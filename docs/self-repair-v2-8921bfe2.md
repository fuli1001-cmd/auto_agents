# 8921bfe2：实施时限和 provider 工具环境

2026-09-15 的 `8921bfe2d3cb4365990973ed` 正确恢复了前一次审查意见。之后完成两次实施及对应审查，并按预算进行了一次重新诊断。最后一次实施从 22:42:38 持续至 23:12:39，触发固定的 1800 秒调用上限；超时前仍在编辑代码，留下未提交修改。

确认的两个问题：

1. 控制器把有实际修改的实施超时直接作为 `provider_failed` 停止，没有让既有验收机制判断这份候选，也没有在重启时优先使用保留的修改。
2. provider 容器用宿主 CLI 目录覆盖了镜像的 PATH，丢失所选 Python 环境。原镜像具备 Python 3.11 和 TOML 解析器，但被覆盖的 PATH 无法找到 Python；本次 agent 转而使用绝对路径下的 Python 3.9，并报告没有 TOML 解析器。原生记录确认本轮实施 effort 是 high，没有发生档位误切换。

现在明确区分超时和主动取消。实施超时且源码变化时，隔离进程停止后保存候选，执行完整的测试保留检查、测试、独立审查及行为对照。任何一项失败仍回到原实施会话，使用既有进展预算；不能因超时获得验收通过或无限重试。无修改超时、其他 provider 错误、规划/审查超时及用户取消不会走这条路径。

provider 保留镜像中的工具链 PATH，CLI 仍通过绝对路径执行。容器无法确认停止时保持阻塞，不将可能继续变化的代码提交为验收候选。前台计数改为“已提交 N 次实施结果”，避免把超时提交误称为完整实施。

原事务的隔离回放已到达测试保留检查，原计划、原生会话和超时修改全部保留，增加零次模型调用；该候选的本地测试保留检查通过。真实 provider 容器中 `python` 已解析到所选 Python 3.11.15，`tomllib` 和 pytest 均可导入。没有调用 Codex 服务，没有启动或修改真实 sdgp 会话；整份候选仍需正式验收。

本地验证记录：`.auto-agents/maintenance/8921bfe2-timeout-replay.json`、`.auto-agents/maintenance/8921bfe2-provider-toolchain.json`。
