# d7383426：测试保留误判与恢复修复

2026-09-15 的任务 `d73834264b964206b94a886e` 在集中验收前停止。该任务沿用了旧候选 `dc0dadebf320e24d01743bb365daa202d154214f`，控制器基线是 `f3277ad`。

## 已确认原因

- 规划模型调用耗时 648.27 秒，实施调用耗时 1565.23 秒；只有这两次模型调用，没有进入正式测试或独立审查。
- 旧候选将 `assert live.read_text() == "original"` 增强为同时检查文件模式，并为归属测试增加 `unscoped` 参数场景。原控制器直接比较断言 AST，因此误报测试削弱。两处变化在本轮 agent 实施前就已存在。
- 原控制器在审计失败时没有记录已完成的实施次数，也没有把问题交回实施会话，而是进入不可恢复的 `tests_weakened` 阻塞状态。
- 规划容器缺少 `dirname`，一次向父目录查找指令文件的 shell 循环不断输出 `dirname: not found`。容器也缺少 `rg`。事件过滤未识别 Codex 的 `outputDelta`，导致 10005 条输出片段逐条进入持久日志。

## 修改后的处理

导入候选在规划前进行本地测试保留检查。实施完成后先保存候选及次数，再执行本地检查；所有具体问题一次性反馈给同一个 writer，消耗原有的有界失败预算。中断和旧版审计阻塞恢复时先复核保存的候选，复核通过即可进入集中验收。

检查按测试及辅助函数分别保护断言，支持合取增强和可静态解析的字面参数扩展。每个原测试的全部断言必须匹配同一个保留参数场景，不能拼凑不同场景的检查。删除旧场景、修改旧预期、引入跳过、变更 indirect 参数或无法确认的改写仍产生修复反馈。正式测试、独立审查、行为对照和原会话恢复检查继续执行。

工具镜像补齐目录、搜索等基础命令，并实际执行工具检查。输出片段不再逐条同步写事件日志，原生会话记录和阶段事件仍保留。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| 修复代码的 V2 回归与前台状态回归，最终 Docker 镜像 | 105 项通过，18.42 秒 |
| 原候选的归属、原始引用和文件内容/模式用例 | 21 项通过，46.78 秒 |
| 原事务的隔离恢复回放 | 到达集中验收入口；增加 0 次模型调用；保留原计划、原生会话和候选 |
| 最终工具镜像 | `auto-agents-verifier:v2-e901641e061142203e1fe7d7`；Python、Conda、dirname、basename、mktemp、rg 实际检查通过 |

详细结果保存在 `/tmp/repair-d738-audit-verification/` 的 `engine-checks-final.json`、`candidate-checks.json` 和 `replay/report.json`。恢复回放主动停在集中验收入口，不代表整份候选已完成验收；本次没有新增 Codex 服务调用，也没有恢复或修改真实 sdgp 会话。

最后一次空间检查中 D 盘可用约 2.14 GiB，接近 2 GiB 保留线；一次新建验证副本已被空间保护正确拦下，后续验证复用已有私有检出并串行执行。已移除本次中间镜像。WSL 内删除文件或镜像不保证 Windows 上的 VHD 文件立即缩小；重新运行长任务前应继续释放宿主盘空间。
