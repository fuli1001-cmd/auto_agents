# Self-repair 3e759d44：收据测试回归与耗尽预算后的修正恢复

## 现场

2026-09-16 的 `collab --session 6a0cedd6674d` 停在 V2 事务
`f3c9521c52c713b51fe705f69652b78dad3f2d815e47353d35155972646b46ee`。
任务 `3e759d4433f849508864e76a` 的阻塞码是 `no_progress`，不是新的 Docker
基础设施错误：累计 6 次实施、14 次 agent 调用、1 次重新诊断，stagnant=2。

候选 `65edf2fea1bb684777ddab0b5b5fa2b679e643b8` 中，生产代码调用
`verification_identity(..., scope=scope)`，但新增的收据策略测试包装器
`_assert_receipt_policy_mismatch.identity` 仍只接收两个位置参数。
独立审查拒绝了该候选，控制器随后触发预算保护。

在隔离 Docker 环境复现收据迁移测试：24 个用例中 8 个因 `scope`
关键字参数报错、16 个通过。将包装器改为接收并转发 `**kwargs`，保留全部
配置恢复断言，24 个用例全部通过。该包装器只存在于保留候选，尚未交付到
安装目录，因此修正必须进入该候选，而不能只改主仓库的控制器。

## 控制器变更

- 新调用可以将新控制器提交合并进耗尽预算的保留候选，直接执行测试保护审计、
  集中测试、独立审查、行为对照及原始恢复边界验收。
- 保留原计划、会话及累计预算。外部修正验收失败时立即停止，不获得额外规划或
  实施轮次。同一调用、已经包含的提交、源码相同的空提交不重开任务。
- 合并前保存意图，合并后进程中断可以继续验收；冲突保留在候选中，不覆盖任何
  一方的工作。其他类型的阻塞不走该恢复分支。
- 原有失败节点、独立审查反例对应的测试优先调度。仍然执行完整测试集合，
  不改变审查标准、测试断言或模型 effort。

## 自动化验证

控制器及公开入口测试 46 项通过，覆盖成功恢复、相同调用、空提交、修正失败、
合并冲突、合并后磁盘写入中断、原始恢复边界失败、完整测试保留与失败优先排序。
超时、取消、测试保护与清理相关测试另有 58 项通过。

完整保留候选的隔离验收记录将在本次验证完成后补充。业务项目和原会话尚未恢复。
