# self-repair 重复复核修复（2026-09-15）

修复提交：`706c25a7801f60f543104a389e98137bebcf1740`。

本次针对 job `b490c9e371b8430a8ed160a3` 的第 97、98 轮。候选代码仍为 `9f3731d4313a6a45f330534cd438afcf6f77a13f`。第 97 轮记录耗时 1648.79 秒，第 98 轮耗时 1018.83 秒；两轮均未新增代码。

第 96 轮曾增加测试，所以第 97 轮重新检查受影响的验收有依据。但它的增量审查要求分类两条历史观察，模型额外复述了四条已经解决的问题。原控制器将 APPROVE 拒收，随后又发起范围审查和完整审查。第 98 轮更直接：审查输入明确记录源码无变化、没有新问题、验收证据有效、36 条命令全部可复用，控制器仍创建了复核候选。

## 调度与证据

`prepare_completed` 重新确认验收有效后，刷新组件及其依赖状态，并返回明确的 COMPLETED 结果。候选入口在预检、测试、模型调用和候选编号生成前退出；搜索循环重新选组，不增加轮次、候选记录或进展分数。真实失败要求的重新验证优先，最终集成和依赖检查仍然执行。

验收封存与复核现在共用一次上下文观察。读取失败记录为“无法观察上下文”，同时保留具体错误；真正发生变化时，记录变化的策略、文件和上下文分项名称。旧日志没有保存发生瞬时差异的分项，无法从旧存档确定第 97 轮结束后每一个变化的来源。此次修复消除了“当前证据已有效却再次复核”的确定性错误，并补齐后续定位证据。

## 审查输出恢复

增量审查和格式修正提供原生结构化回复约束，分类数量和 ID 绑定当前待审问题。空问题列表对应空分类列表。

重复的历史分类只有同时满足以下条件才可从当前分类列表中排除：属于已认证验收记录的 resolved 集合；问题指纹没有变化、没有重新打开；结论为 not_applicable；具有具体反证和有效证据。原模型回复保持原样，额外分类不会写成新的范围豁免。新增、未知、冲突或必修问题不会被静默丢弃。

其他格式错误在原审查上做局部修正，最多两次；已消耗次数和原回复通过有摘要校验的记录跨重启保留。修改未报告的实质性字段会被拒绝。真实缺陷继续进入完整审查。格式恢复耗尽会作为 planning_blocked 返回，不再被算成一轮新的代码修复失败。

## 验证

- 主修复分支的 self-repair 与引擎恢复相关回归：683 项通过，0 失败、0 跳过，约 153 秒。
- 旧 `682bfae` 的调度函数放回相同测试框架：两项新增回归均按预期失败，分别复现无必要的预检和重复调度；新实现通过。
- 第 97 轮真实回复回放：旧验证器拒收六条分类；新实现保留两条所需分类的全部内容，验证通过。原始回复与存档内容未改变。
- 第 98 轮存档匹配“有效验收直接重新选组”的回归场景，确认 36 条可复用检查和零源码变化。
- 使用真实 `carry_continuous_work` 在隔离副本中接续候选，新修复与原候选无冲突合并，两个提交均为合并结果的祖先。合并结果为 `06b235efd665fe4d0547b762d1e46a19318f029d`。

- 合并后的候选使用原修复的 8518272c2c1eafbfc0bf1fdc Python 环境执行恢复相关回归：114 项通过，0 失败、0 跳过，约 35 秒。

机器可读结果见 [验证报告](self-repair-revalidation-validation.json)。存档回放命令由 `scripts/replay_repair_revalidation.py` 提供，全程不调用模型，也不恢复原项目。

第 98 轮这种重复工作可直接省去该轮约 17 分钟的流水线。第 97 轮减少了额外模型审查，但测试与审查并行，不能把模型耗时直接相加作为总提速。完整 self-repair 的总耗时仍需后续实际运行测量。
