# Self-repair V2 开发状态

2026-09-15：从 WSL 空间不足中断恢复。代码保存在 `self-repair-v2-20260915` 分支，入口仅由控制器配置 `repair_engine=v2` 开启，尚未切换生产默认路径。

已实现整体 plan → 连续实施 → 冻结候选 → 测试与独立审查并行的原型；有持久检查点、停滞预算、源码/收据校验、原生 CLI 适配和 Docker 验证。恢复中补上候选未提交修改导入、空间检查、临时副本生命周期和审查格式纠正不能更改实质结论的约束。

## 本轮实测

- 通用磁盘/快照及相关引擎回归：212 passed，2 subtests passed。
- 后续 V2 聚焦回归：35 passed。
- 真实 Docker 通过/失败测试均清除工作副本，保留结果日志。
- 复用现有镜像耗时 5.23 秒，没有新构建，也没有展开或保留 rootfs。
- Codex 合成别名修复：215.33 秒，1 次实施，3 次调用，独立审查和测试通过。测量发生在开发过程中，不是全部最终变更的端到端验收。
- Antigravity：已修复遗漏无扩展名认证文件及强行注入不兼容 effort 参数；本机配置的 `Gemini 3.5 Flash (High)` 被当前 CLI 报为未知模型，验证停止。
- Copilot：已修复 JSONC 配置被漏读；当前真实调用被供应商返回 `Access denied by policy settings`，验证停止。
- Claude：本轮没有重新完成全流程真实验证。

这些结果不构成 75% 提速承诺，也不构成四种 provider 全部支持完成的证明。供应商失败未计为成功，未自动更换模型或修改账户策略，未恢复 sdgp 会话。

## 尚未完成

1. 固定控制器下的发布、上游变更整合验证和原会话恢复，彻底消除交付阶段回到旧候选执行器的路径。
2. 更完整的跨版本依赖闭包证明与缓存审计；不透明依赖保持重跑。
3. provider 认证/模型前置探测，以及四种 CLI 的完整协议与恢复验证。
4. 持久快照、历史证据和镜像的引用/保留策略；目前空间不足时保留数据并停止，不跨越证据保护规则删除。
5. 原始复杂案例、跨模块案例和中断恢复案例的多轮配对基准，验证目标速度与质量后才允许切换默认引擎。

生产环境当前可用的是 `master` 上的通用磁盘防护，不能将本分支原型当作已完成的生产升级。
