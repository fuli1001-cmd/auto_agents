# Self-repair V2 状态

完整实现见 [Self-repair V2：统一规划、连续实施、集中验收](self-repair-v2.md)，实测记录见 [验收数据](self-repair-v2-validation.json)。

实现提交 `e686ec1`，发布收尾修复 `f1893ff`。默认入口使用 V2，保留旧候选和原会话，通过固定控制器完成规划、实施、集中验收、交付及恢复。

完整 Docker 回归覆盖 185 个批次。发现的可选镜像清理问题已修复；受影响的发布和清理在真实 Docker 中复测 15 项全部通过。另有 167 项针对性回归通过。

Codex CLI 三组配对样例：旧流程中位约 648 秒，新流程约 172 秒，耗时减少约 73.5%。75% 性能目标尚未达到，不能将小型样例的比例直接用于原始复杂修复。

原生中断恢复、跨模块命令归属/选择器/结果保留样例，以及原始 sdgp 会话的隔离恢复检查均通过；没有恢复或修改原项目。按用户要求，仅真实验收 Codex CLI，其他 provider 保留适配和离线协议测试。
