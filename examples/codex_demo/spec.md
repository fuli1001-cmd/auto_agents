# Spec

Build a tiny Python CLI called `greeter`.

Requirements:

- Running `python3 greeter.py` prints `hello`
- Add one unit test under `tests/`
- Keep the project minimal and avoid extra dependencies
